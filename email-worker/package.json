{
  "name": "trueseal-dkim-selector-test",
  "version": "1.0.0",
  "private": true,
  "description": "Standalone Cloudflare Email Worker that extracts the DKIM selector/domain from incoming mail. Not wired into the main TrueSeal app.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
